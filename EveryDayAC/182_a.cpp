#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll INF = 1LL << 60;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main () {
    int A, B;
    cin >> A >> B;
    cout << 2 * A + 100 - B << endl;
    return 0;
}