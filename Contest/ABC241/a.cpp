#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    vector<ll> vec(10);
    for (int i = 0; i < 10; ++i){
        cin >> vec[i];
    }
    ll num = 0;
    num = vec[num];
    num = vec[num];
    num = vec[num];
    cout << num << endl;
    return 0;
}