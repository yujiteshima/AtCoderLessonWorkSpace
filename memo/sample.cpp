#include <bits/stdc++.h>
using namespace std;

int main () {
    // cout << (10 % 3 < 0) << endl;
    // cout << (-10 % 3 < 0) << endl;
    cout << char((('a' - 'a') + 1) % 26 + 'a') << endl;
    cout << (('a' - 'a') + 1) % 26 + 'a' << endl;
    cout << '9' - '0' << endl;
    cout << 1 + 'a' << endl;
    return 0;
}